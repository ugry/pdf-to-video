# Install PyTorch with CUDA first (see install.sh)
# torch and torchvision are NOT listed here — install them separately

diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0
pdfplumber>=0.10.0
kokoro>=0.9.2
soundfile>=0.12.0
moviepy==1.0.3
ollama>=0.1.7
numpy>=1.24.0
scipy>=1.11.0
imageio>=2.33.0
imageio-ffmpeg>=0.4.9
Pillow>=10.0.0
# xformers is installed separately in install.sh (version must match torch)
